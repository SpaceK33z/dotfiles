# TODO: This should only be executed on Debian systems, so for now, run manually.
zstyle ':completion:*:apt-get:*' use-cache on
zstyle ':completion:*:apt-cache:*' use-cache on
zstyle ':completion:*:dpkg:*' use-cache on
zstyle ':completion:*:aptitude:*' use-cache on
